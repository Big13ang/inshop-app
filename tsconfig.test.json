{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@/*": ["./*"]
    },
    // node16 resolves Node.js subpath exports correctly (msw/node, stream/web, etc.)
    // The main tsconfig uses "bundler" which is optimised for Next.js/webpack — not Jest.
    "moduleResolution": "bundler",
    "module": "esnext",
    // Make Jest globals (describe, it, expect, beforeAll, etc.) available
    "types": ["jest", "node", "@testing-library/jest-dom"],
    // Test files don't need to emit — type-checking only
    "noEmit": true
  },
  "include": [
    // Test infrastructure
    "jest.setup.ts",
    "jest.polyfills.js",
    "jest.config.js",
    // Mock server files
    "mocks/**/*.ts",
    // All test files
    "**/__tests__/**/*.ts",
    "**/__tests__/**/*.tsx",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/*.spec.ts",
    "**/*.spec.tsx",
    // Source files for type resolution
    "features/**/*.ts",
    "features/**/*.tsx",
    "components/**/*.ts",
    "components/**/*.tsx",
    "lib/**/*.ts",
    "lib/**/*.tsx",
    "app/**/*.ts",
    "app/**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    "temp",
    ".next"
  ]
}
